import { SubmissionStatusLabelPipe } from './submission-status-label.pipe';

describe('SubmissionStatusLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new SubmissionStatusLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
