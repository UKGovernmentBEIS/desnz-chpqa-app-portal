import { SubmissionGroupTypeLabelPipe } from './submission-group-type-label.pipe';

describe('SubmissionGroupTypeLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new SubmissionGroupTypeLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
