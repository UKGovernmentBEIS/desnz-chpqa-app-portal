import { SubmissionGroupCategoryLabelPipe } from './submission-group-category-label.pipe';

describe('SubmissionGroupCategoryLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new SubmissionGroupCategoryLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
